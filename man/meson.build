# Man pages
# First all other languages than English
foreach locale : ['fr']
    locale_file = locale / 'atomes.1'
    install_man (locale_file, locale: locale)
endforeach

# English translation
install_man ('en/atomes.1')
